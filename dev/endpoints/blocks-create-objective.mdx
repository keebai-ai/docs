---
title: "POST /v1/blocks/objective"
sidebarTitle: "POST blocks/objective"
description: "Create a objective block with its content as plain text."
openapi: "POST /v1/blocks/objective"
---

What the agent has to achieve in the conversation, and how you know it did. Keeps an agent from being helpful in general and useless in particular.

The block is created in the project catalogue and is **not attached to anything**. Wire it into an agent with [`POST /v1/agents/{id}/blocks`](/dev/endpoints/agents-block-attach) afterwards.

<Note>
  **Identity block: one per agent.** A second `objective` on the same agent fails with `ASSISTANT_DUPLICATE_IDENTITY_BLOCK`.
</Note>

## Endpoint

```
POST https://api.keebai.com/v1/blocks/objective
```

## Required scope

`agents:write`

## Headers

| Header | Required | Value |
|---|---|---|
| `Authorization` | Yes | `Bearer kbai_pk_<token>` |
| `Content-Type` | Yes | `application/json` |

## Body

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | `string` | Yes | Up to 150 characters. What the block shows as in the portal. |
| `content` | `string` | No | The block's text, up to 100,000 characters. Omit it to create an empty block. |

`type` and `family` are not accepted: the type is the endpoint you called, and `family` is derived from it (`identity` for this one).

## Example request

<CodeGroup>

```bash curl
curl -X POST https://api.keebai.com/v1/blocks/objective \
  -H "Authorization: Bearer kbai_pk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Objetivo",
    "content": "# Objetivo\nCerrar una reserva en el primer intercambio de mensajes.\n\n## Se logró cuando\n- El cliente eligió servicio, día y hora.\n- Quedó registrada la reserva."
  }'
```

```js JavaScript
const resp = await fetch("https://api.keebai.com/v1/blocks/objective", {
  method: "POST",
  headers: {
    Authorization: `Bearer ${process.env.KEEBAI_API_TOKEN}`,
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    name: "Objetivo",
    content: "# Objetivo\nCerrar una reserva en el primer intercambio de mensajes.\n\n## Se logró cuando\n- El cliente eligió servicio, día y hora.\n- Quedó registrada la reserva.",
  }),
});

const { block } = await resp.json();

// Then wire it into an agent.
await fetch("https://api.keebai.com/v1/agents/6650a1b2c3d4e5f6a7b8c9d0/blocks", {
  method: "POST",
  headers: {
    Authorization: `Bearer ${process.env.KEEBAI_API_TOKEN}`,
    "Content-Type": "application/json",
  },
  body: JSON.stringify({ block_id: block._id }),
});
```

```python Python
import os, requests

resp = requests.post(
    "https://api.keebai.com/v1/blocks/objective",
    headers={"Authorization": f"Bearer {os.environ['KEEBAI_API_TOKEN']}"},
    json={"name": "Objetivo", "content": "# Objetivo\nCerrar una reserva en el primer intercambio."},
    timeout=10,
)
resp.raise_for_status()
block = resp.json()["block"]
```

</CodeGroup>

## Response

### 201 Created

```json
{
  "block": {
    "_id": "6650bbbb2222cccc3333dddd",
    "type": "objective",
    "family": "identity",
    "name": "Objetivo",
    "current_version_id": "6650ffff4444aaaa5555bbbb",
    "is_archived": false,
    "content_writable": true,
    "created_at": "2026-07-26T21:38:22.104Z",
    "updated_at": "2026-07-26T21:38:22.104Z"
  }
}
```

### 400 Bad Request

Missing `name`, `content` over 100,000 characters, or a property the endpoint does not accept.

### 401 Unauthorized

Missing, invalid, revoked, or expired token.

### 403 Forbidden

The token does not have the `agents:write` scope.

### 502 Bad Gateway

The agent service is unreachable.

## Operational notes

- An objective works best when it names a single outcome. Two competing objectives in one block tend to produce an agent that pursues neither.
- **Creating a block also creates its first version.** The response already carries a `current_version_id`, even when you omit `content` — the block starts with one empty version rather than none.
- **Creation is not idempotent.** Nothing enforces unique names, so a retry after a timeout leaves you with two blocks. Check [the catalogue](/dev/endpoints/blocks-list) filtered by `type` before retrying.
- **Content is plain text with three markdown conventions**: `-` for bullets, `1.` for numbered items, `#` for headings. Everything else is a paragraph. [More on how content is stored](/dev/agents/overview#content-is-text).
- **The block belongs to the token's project**, and a token on another project can neither see it nor read it by id.
