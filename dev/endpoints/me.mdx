---
title: "GET /v1/me"
sidebarTitle: "GET /v1/me"
description: "Devuelve los permisos efectivos del Personal Access Token autenticado. Útil para validar que el token funciona y descubrir qué endpoints podés llamar."
---

Devuelve los **permisos efectivos** del token autenticado: la intersección entre los scopes con los que se creó el token y los permisos que el usuario dueño tiene en este momento.

Sirve principalmente para:

- Validar al inicio de tu integración que el token está vivo y tiene los scopes que esperás.
- Mostrar al usuario final (en una UI de configuración de tu app) qué acciones va a poder hacer.
- Detectar si el admin de tu tenant te quitó un permiso (el scope desaparecerá del array).

## Endpoint

```
GET https://api.keebai.com/v1/me
```

## Headers

| Header | Requerido | Valor |
|---|---|---|
| `Authorization` | Sí | `Bearer kbai_pk_<token>` |

No requiere body, query params ni path params.

## Ejemplo de request

<CodeGroup>

```bash curl
curl https://api.keebai.com/v1/me \
  -H "Authorization: Bearer kbai_pk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
```

```js JavaScript
const response = await fetch("https://api.keebai.com/v1/me", {
  headers: { Authorization: `Bearer ${process.env.KEEBAI_API_TOKEN}` },
});
const { permissions } = await response.json();
```

```python Python
import os, requests

resp = requests.get(
    "https://api.keebai.com/v1/me",
    headers={"Authorization": f"Bearer {os.environ['KEEBAI_API_TOKEN']}"},
    timeout=10,
)
resp.raise_for_status()
permissions = resp.json()["permissions"]
```

</CodeGroup>

## Respuesta

### 200 OK

```json
{
  "permissions": [
    "conversations.read",
    "contacts.read",
    "crm.read"
  ]
}
```

| Campo | Tipo | Descripción |
|---|---|---|
| `permissions` | `string[]` | Permisos efectivos del token. Es la intersección entre los scopes del token y los permisos actuales del usuario dueño. Puede estar vacío si el usuario perdió todos los permisos relevantes desde que el token se creó. |

### 401 Unauthorized

Token inválido, revocado o expirado. Ver [Errores de autenticación](/dev/authentication#errores-de-autenticación).

```json
{
  "message": "Invalid or missing API token",
  "error": "Unauthorized",
  "statusCode": 401
}
```

### 429 Too Many Requests

Pasaste el rate limit. Ver [Rate limits](/dev/rate-limits).

## Patrones comunes

### Validar al inicio de la integración

```python
import os, requests, sys

REQUIRED = {"conversations.read", "contacts.read"}

resp = requests.get(
    "https://api.keebai.com/v1/me",
    headers={"Authorization": f"Bearer {os.environ['KEEBAI_API_TOKEN']}"},
    timeout=10,
)
if resp.status_code == 401:
    sys.exit("Token inválido o revocado")
resp.raise_for_status()

effective = set(resp.json()["permissions"])
missing = REQUIRED - effective
if missing:
    sys.exit(f"Faltan permisos: {sorted(missing)}")

print("Token OK con permisos:", sorted(effective))
```

### Health check de tu integración

Llamar `GET /v1/me` cada 5 minutos como liveness probe interno: si recibís 401 reiteradamente, alertás a tu equipo de que el token fue revocado o expiró.
